# Fast factorization in diagram monoids
This repository collects all code used for the paper "Fast factorization in diagram monoids" [INSERT].

# Usage
To factor a particular element of a diagram monoid in bipartition form use the functions [Monoid]Fact(bipartition);

To time the factorization over a certain number of runs use Time[Monoid]Fact(n,runs);
